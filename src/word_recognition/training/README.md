# training

Raw-to-feature conversion, model training, and training artifact download.

Current routes:

- `POST /dataset/raw/convert-to-features`
- `POST /model/train`
- `GET /artifacts/{run_name}/{file_name}`
